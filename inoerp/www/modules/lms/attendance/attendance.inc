<?phpif (empty($$class->lms_attendance_id)) { $$class->status = 'ENTERED'; if (!empty($_SESSION['user_id']) && empty($$class->lms_student_id)) {  $user_details = ino_user::find_by_id($_SESSION['user_id']);  $$class->lms_student_id = $user_details->lms_student_id; }}if (!empty($$class->lms_student_id)) { $student_details = lms_student::find_by_id($$class->lms_student_id);  $$class->student_name = $student_details->first_name . ' ' . $student_details->last_name;}?>