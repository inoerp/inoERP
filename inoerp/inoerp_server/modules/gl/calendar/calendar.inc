<?phpif (!empty($_GET['option_line_code'])) { $option_line_code_h = $_GET['option_line_code']; $calendar_object = gl_calendar::find_by_optionLineCode($option_line_code_h);} else { $option_line_code_h = null; }if(empty($calendar_object)){ $calendar_object = [new gl_calendar()];}$pageno = !empty($_GET['pageno']) ? $_GET['pageno'] : 1;$per_page = !empty($_GET['per_page']) ? $_GET['per_page'] : 10;$total_count = count($calendar_object);$pagination = new pagination($pageno, $per_page, $total_count);$pagination->setProperty('_path', 'form');$position = ($pageno - 1) * $per_page;?>