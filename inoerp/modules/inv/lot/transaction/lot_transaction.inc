<?php $mode = 2; $readonly = 1; $lot_number_h = null; $inv_lot_number_id_h = null; $inv_lot_transaction_object = []; if (!(empty($search_result))) {  $lot_number_h = $search_result[0]->lot_number;  $inv_lot_number_id_h = $search_result[0]->inv_lot_number_id;  $lot_info = new inv_lot_number();  $lot_info->findBy_id($inv_lot_number_id_h);  foreach($lot_info as $key => $value ){   if(property_exists($$class, $key)){    $$class->$key = $value;   }  }  foreach ($search_result as $object) {   array_push($inv_lot_transaction_object, $object);  }  if (!empty($pagination)) {   $pagination->setProperty('_path', 'form');   $pagination_statement = $pagination->show_pagination();  } } else {  $lot_trnx = new inv_lot_transaction_v();  array_push($inv_lot_transaction_object, $lot_trnx); }?>