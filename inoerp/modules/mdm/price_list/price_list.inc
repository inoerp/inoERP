<?phpif (!empty($_GET['mdm_price_list_header_id']) && empty($mdm_price_list_line_object)) { $mdm_price_list_line_object = mdm_price_list_line::find_by_parent_id($_GET['mdm_price_list_header_id']);} if(empty($mdm_price_list_line_object)){ $mdm_price_list_line_object = [new mdm_price_list_line()];}$pageno = !empty($_GET['pageno']) ? $_GET['pageno'] : 1;$per_page = !empty($_GET['per_page']) ? $_GET['per_page'] : 10;$total_count = count($mdm_price_list_line_object);$pagination = new pagination($pageno, $per_page, $total_count);$pagination->setProperty('_path', 'form');$position = ($pageno - 1) * $per_page;if (!empty($mdm_price_list_line_object)) { foreach ($mdm_price_list_line_object as &$price_line) {	if (!empty($price_line->item_id_m)) {	 $item = item::find_by_item_id_m($price_line->item_id_m);	 $price_line->item_number = $item->item_number;	 $price_line->item_description = $item->item_description;	} else {	 $price_line->item_number = null;	 $price_line->item_description = null;	} }}?>