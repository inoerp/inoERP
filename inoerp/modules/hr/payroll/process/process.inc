<?phpif (!empty($$class->hr_payroll_process_id)) { $hr_payslip_header_object = hr_payslip_header::find_by_payrollProcessId($$class->hr_payroll_process_id); $total_count = count($hr_payslip_header_object);}else{ $total_count = 0;}if(empty($hr_payslip_header_object)){ $hr_payslip_header_object = array(new hr_payslip_header());} $pageno = !empty($_GET['pageno']) && !is_array($_GET['pageno']) ? $_GET['pageno'] : 1; $per_page = (!empty($_GET['per_page'])  && !is_array($_GET['per_page'])) ? $_GET['per_page'] : 10; $pagination = new pagination($pageno, $per_page, $total_count); $pagination->setProperty('_path', 'form.php?'); $position = ($pageno - 1) * $per_page;?>