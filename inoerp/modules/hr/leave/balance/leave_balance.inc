<?php if (!empty($_GET['employee_id'])) {  $employee_id_h = $_GET['employee_id'];  $$class->employee_id = $employee_id_h; } else {  $employee_id_h = null; } if (!empty($$class->employee_id)) {  $emp_details = hr_employee::find_by_id($$class->employee_id);  $$class->identification_id = $emp_details->identification_id;  $$class->employee_name = $emp_details->first_name . ' ' . $emp_details->last_name; } $leave_balance_object = []; if (!(empty($search_result))) {  foreach ($search_result as $object) {   array_push($leave_balance_object, $object);  }  if (!empty($pagination)) {   $pagination->setProperty('_path', 'form');   $pagination_statement = $pagination->show_pagination();  } } else if (!(empty($$class))) {  array_push($leave_balance_object, $$class); } else {  $leave_balance = new hr_leave_balance;  array_push($leave_balance_object, $leave_balance); }?>