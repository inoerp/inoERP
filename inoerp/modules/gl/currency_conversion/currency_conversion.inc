<?phpif (!empty($_GET['currency_conversion_type'])) { $currency_conversion_type_h = $_GET['currency_conversion_type']; $currency_conversion_object = gl_currency_conversion::find_by_conversion_type($currency_conversion_type_h); pa($currency_conversion_object);}else{ $currency_conversion_type_h = null;}if(empty($currency_conversion_object)){ $currency_conversion_object = [new gl_currency_conversion()];}$pageno = !empty($_GET['pageno']) ? $_GET['pageno'] : 1;$per_page = !empty($_GET['per_page']) ? $_GET['per_page'] : 10;$total_count = count($currency_conversion_object);$pagination = new pagination($pageno, $per_page, $total_count);$pagination->setProperty('_path', 'form');$position = ($pageno - 1) * $per_page;?>