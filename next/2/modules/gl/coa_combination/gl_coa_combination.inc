<?phpif (!empty($_GET['coa_id'])) { $coa_id_h = $_GET['coa_id']; $coa_combination_object = coa_combination::find_all_from_coa($coa_id_h);} else if (!empty($_GET['coa_combination_id'])) { $coa_combination_i = coa_combination::find_by_id($_GET['coa_combination_id']); $coa_combination_object = array($coa_combination_i); $coa_id_h = $coa_combination_i->coa_id;} else { $coa_id_h = null;}if (empty($coa_combination_object)) { $coa_combination_object = [new coa_combination()];}$pageno = !empty($_GET['pageno']) ? $_GET['pageno'] : 1;$per_page = !empty($_GET['per_page']) ? $_GET['per_page'] : 10;$total_count = count($coa_combination_object);$pagination = new pagination($pageno, $per_page, $total_count);$pagination->setProperty('_path', 'form');$position = ($pageno - 1) * $per_page;?><?php//coa_structure_id is same as option_header_id//if (!empty($_GET["coa_id"]) && (!is_array($_GET["coa_id"]))) {// $coa_id = htmlentities($_GET["coa_id"]);//}//if ((empty($search_result)) && (empty($_GET["coa_id"])) && (!empty($_GET['coa_structure_id']))) {// $coa_structure_id = $_GET['coa_structure_id'];// if (count(coa::find_by_structure($coa_structure_id)) > 0) {//	$coa = coa::find_by_structure($coa_structure_id);// }//}////if(!isset($$class_second)){// $class_second = 'coa';// $$class_second = new $class_second ;//}////if (!(empty($search_result))) {// $$class_second->coa_id = $search_result[0]->coa_id;// $$class_second->coa_structure_id = coa::find_by_id($$class_second->coa_id)->coa_structure_id;// $coa_combination_object = [];// foreach ($search_result as $object) {//	array_push($coa_combination_object, $object);// }// if (!empty($pagination)) {//	$pagination->setProperty('_path', 'form');//	$pagination_statement = $pagination->show_pagination();// }//} else if (!empty($$class->coa_combination_id)) {// $$class_second->coa_id = $$class->coa_id;// $$class_second->coa_structure_id = coa::find_by_id($$class_second->coa_id)->coa_structure_id;// $coa_combination_object = array();// array_push($coa_combination_object, $coa_combination);//} else if ((isset($$class_second))&&(!empty($$class_second->coa_id))) {// $coa_combination_object = coa_combination::find_coa_combination_by_coa_id($$class_second->coa_id);// if (count($coa_combination_object) == 0) {//	$coa_combination = new coa_combination;//	$coa_combination_object = array();//	array_push($coa_combination_object, $coa_combination);// }//} else {// $coa_combination = new coa_combination;// $coa_combination_object = array();// array_push($coa_combination_object, $coa_combination);//}?>