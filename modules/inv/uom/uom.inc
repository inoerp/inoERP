<?php$module_info = [		array(				"module" => "org",				"class" => "uom",				"key_field" => "uom",				"primary_column" => "uom_id"		)];$pageTitle = " Uom - Create & Update all Uom ";$view_path = "uom_view";?><?php include_once("../../../include/basics/header.inc"); ?> <script src="uom.js"></script><?phpif (!(empty($search_result))) { $search_result_statement = search::search_result($class, $column_array, $search_result, $primary_column, $view_path);  if(!empty($pagination)){$pagination_statement = $pagination->show_pagination($pagination, 'uoms', $pageno, $query_string );}}elseif(!empty($uom)){	$search_result = array();	array_push($search_result, $uom);}else{  $form_line = new uom;	$search_result = array();	array_push($search_result, $form_line);}if (!empty($msg)) { $show_message = '<div class="error">'; foreach ($msg as $key => $value) {	$x = $key + 1;	$show_message .= 'Message ' . $x . ' : ' . $value . '<br />'; } $show_message .= '</div>';}?>