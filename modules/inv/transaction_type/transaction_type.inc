<?phpif (!(empty($search_result))) { $search_result_statement = search::search_result($class, $column_array, $search_result, $primary_column, $view_path); if(!empty($pagination)){$pagination_statement = $pagination->show_pagination($pagination, 'transaction_types', $pageno, $query_string );}}elseif(!empty($transaction_type)){	$search_result = array();	array_push($search_result, $transaction_type);}else{  $form_line = new transaction_type;	$search_result = array();	array_push($search_result, $form_line);}?>