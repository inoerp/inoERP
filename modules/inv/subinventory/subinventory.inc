<?php$module_info = [		array(				"module" => "org",				"class" => "subinventory",				"key_field" => "org_id",				"primary_column" => "subinventory_id"		)];$pageTitle = " Sub Inventory - Create & Update all Subinventories ";$view_path = "subinventory_view";?><?php include_once("../../../include/basics/header.inc"); ?> <script src="subinventory.js"></script><?phpif (!(empty($search_result))) { foreach ($search_result as $object) {	$org = org::find_by_id($object->org_id);//	$object->org = $org->org; } $search_result_statement = search::search_result($class, $column_array, $search_result, $primary_column, $view_path); if(!empty($pagination)){$pagination_statement = $pagination->show_pagination($pagination, 'subinventorys', $pageno, $query_string );}}elseif(!empty($subinventory)){	$search_result = array();	array_push($search_result, $subinventory);}else{  $form_line = new subinventory;	$search_result = array();	array_push($search_result, $form_line);}$coa_id = 7;$all_code_combinations = coa_combination::find_all_from_coa($coa_id);function account_field($account_name) { global $all_code_combinations, $subinventory, $readonly; echo form::select_field_from_object($account_name, $all_code_combinations, 'coa_combination_id', 'combination', $subinventory->$account_name, $account_name, $readonly);}if (!empty($msg)) { $show_message = '<div class="error">'; foreach ($msg as $key => $value) {	$x = $key + 1;	$show_message .= 'Message ' . $x . ' : ' . $value . '<br />'; } $show_message .= '</div>';}?>