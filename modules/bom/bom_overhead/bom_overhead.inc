<?php$module_info = [		array(				"module" => "bom",				"class" => "bom_overhead",				"key_field" => "overhead",				"primary_column" => "bom_overhead_id"		), array(				"module" => "bom",				"class" => "bom_overhead_resource_assignment",				"key_field" => "resource_id",				"primary_column" => "bom_overhead_resource_assignment_id"		),		array(				"module" => "bom",				"class" => "bom_overhead_rate_assignment",				"key_field" => "department_id",				"primary_column" => "bom_overhead_rate_assignment_id"		)//	=];$pageTitle = " Overhead - Create & Update all Material/Resouce Overhead Elements ";$view_path = "bom_overhead_view";?><?php include_once("../../../include/basics/header.inc"); ?> <script type='text/javascript' src="bom_overhead.js" ></script><?php$class = $class_first = 'bom_overhead';$$class = $$class_first = &$bom_overhead;$class_second = 'bom_overhead_resource_assignment';$$class_second = &$bom_overhead_resource_assignment;$class_third = 'bom_overhead_rate_assignment';$$class_third = &$bom_overhead_rate_assignment;?><?phpif (!empty($_GET["bom_overhead_id"])) { $bom_overhead_id = htmlentities($_GET["bom_overhead_id"]);} elseif (!empty($_POST["bom_overhead_id"][0])) { $bom_overhead_id = $_POST["bom_overhead_id"][0];} else { $bom_overhead_resource_assignment = new bom_overhead_resource_assignment(); $bom_overhead_resource_assignment_object = array(); array_push($bom_overhead_resource_assignment_object, $bom_overhead_resource_assignment); $bom_overhead_rate_assignment = new bom_overhead_rate_assignment(); $bom_overhead_rate_assignment_object = array(); array_push($bom_overhead_rate_assignment_object, $bom_overhead_rate_assignment);}if (!empty($bom_overhead_id)) { $bom_overhead_resource_assignment_object = bom_overhead_resource_assignment::find_by_bom_overheadId($bom_overhead_id); if (count($bom_overhead_resource_assignment_object) == 0) {	$bom_overhead_resource_assignment = new bom_overhead_resource_assignment;	$bom_overhead_resource_assignment_object = array();	array_push($bom_overhead_resource_assignment_object, $bom_overhead_resource_assignment); }   $bom_overhead_rate_assignment_object = bom_overhead_rate_assignment::find_by_bom_overheadId($bom_overhead_id); if (count($bom_overhead_rate_assignment_object) == 0) {	$bom_overhead_rate_assignment = new bom_overhead_rate_assignment;	$bom_overhead_rate_assignment_object = array();	array_push($bom_overhead_rate_assignment_object, $bom_overhead_rate_assignment); }}?><?phpif (!empty($msg)) { $show_message = '<div class="error">'; foreach ($msg as $key => $value) {	$x = $key + 1;	$show_message .= 'Message ' . $x . ' : ' . $value . '<br />'; } $show_message .= '</div>';}?>