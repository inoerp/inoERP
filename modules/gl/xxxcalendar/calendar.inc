<?phpob_start();$module_info = [		array(				"module" => "gl",				"class" => "gl_calendar",				"key_field" => "name",				"primary_column" => "gl_calendar_id"		)];$pageTitle = " Calendar  - Create & Update all Calendar ";$view_path = "calendar_view";?><?php include_once("../../../include/basics/header.inc"); ?> <script type='text/javascript' src="calendar.js" ></script><?phpglobal $f;$class = $class_first = 'gl_calendar';$$class = $$class_first = &$gl_calendar;?><?phpif(!empty($_GET['option_line_code'])){ $option_line_code = $_GET['option_line_code'];}if (!(empty($search_result))) { $calendar_object = []; foreach ($search_result as $object) {	array_push($calendar_object, $object); } if (!empty($pagination)) {	$pagination_statement = $pagination->show_pagination($pagination, 'calendar', $pageno, $query_string); }} else { $calendar_object = []; $calendar = new gl_calendar;  if(!empty($option_line_code)){	$calendar->option_line_code = $option_line_code; } array_push($calendar_object, $calendar);}?>