<?php$module_info = [		array(				"module" => "org",				"class" => "business",				"key_field" => "org_id",				"primary_column" => "business_id"		)];$pageTitle = " Business Org - Create & Update all business_orgs ";$view_path = "business_view";?><?php include_once("../../../include/basics/header.inc"); ?> <script src="business.js"></script><?php if(!(empty($search_result))){foreach ($search_result as $object){ $org = org::find_by_id($object->org_id); $object->org =  $org->org; $object->description =  $org->description;}$search_result_statement = search::search_result($class, $column_array, $search_result, $primary_column, $view_path);}if (!empty($msg)) { $show_message = '<div id="dialog_box">'; foreach ($msg as $key => $value) {	$x = $key + 1;	$show_message .= 'Message ' . $x . ' : ' . $value . '<br />'; } $show_message .= '</div>';}?>