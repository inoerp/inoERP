<?php$module_info = [		array(				"module" => "enterprise",				"class" => "enterprise",				"key_field" => "org_id",				"primary_column" => "enterprise_id"		)];$pageTitle = " Enterprise - Create & Update all enterprises ";$view_path = "enterprise_view";?><?php include_once("../../../include/basics/header.inc"); ?> <script src="enterprise.js"></script><?php if(!(empty($search_result))){foreach ($search_result as $object){ $org = org::find_by_id($object->org_id); $object->org =  $org->org; $object->description =  $org->description;}$search_result_statement = search::search_result($module, $column_array, $search_result, $primary_column, $view_path);}if (!empty($msg)) { $show_message = '<div id="dialog_box">'; foreach ($msg as $key => $value) {	$x = $key + 1;	$show_message .= 'Message ' . $x . ' : ' . $value . '<br />'; } $show_message .= '</div>';}?>