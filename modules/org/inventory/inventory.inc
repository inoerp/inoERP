<?php$module_info = [		array(				"module" => "org",				"class" => "inventory",				"key_field" => "org_id",				"primary_column" => "inventory_id"		)];$pageTitle = " Inventory Org - Create & Update all inventory_orgs ";$view_path = "inventory_view";?><?php include_once("../../../include/basics/header.inc"); ?> <script src="inventory.js"></script><?phpif (!(empty($search_result))) { foreach ($search_result as $object) {	$org = org::find_by_id($object->org_id);	$object->org = $org->org;	$object->description = $org->description; } $search_result_statement = search::search_result($class, $column_array, $search_result, $primary_column, $view_path);}$coa_id = 7;$all_code_combinations = coa_combination::find_all_from_coa($coa_id);function account_field($account_name) { global $all_code_combinations, $inventory, $readonly; echo form::select_field_from_object($account_name, $all_code_combinations, 'coa_combination_id', 'combination', $inventory->$account_name, $account_name, $readonly);}if (!empty($msg)) { $show_message = '<div id="dialog_box">'; foreach ($msg as $key => $value) {	$x = $key + 1;	$show_message .= 'Message ' . $x . ' : ' . $value . '<br />'; } $show_message .= '</div>';}?>