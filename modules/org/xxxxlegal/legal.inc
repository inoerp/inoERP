<?php$module_info = [		array(				"module" => "org",				"class" => "legal",				"key_field" => "org_id",				"primary_column" => "legal_id"		)];$pageTitle = " Legal Org - Create & Update all legal_orgs ";$view_path = "legal_view";?><?php include_once("../../../include/basics/header.inc"); ?> <script src="legal.js"></script><?php if(!(empty($search_result))){foreach ($search_result as $object){ $org = org::find_by_id($object->org_id); $object->org =  $org->org; $object->description =  $org->description;}$search_result_statement = search::search_result($class, $column_array, $search_result, $primary_column, $view_path);}// function text_field_d($field_name){// global $readonly, $class, $$class;// echo form::text_field($field_name, $$class->$field_name, '30', '', '', '', $field_name, $readonly);// } if (!empty($msg)) { $show_message = '<div id="dialog_box">'; foreach ($msg as $key => $value) {	$x = $key + 1;	$show_message .= 'Message ' . $x . ' : ' . $value . '<br />'; } $show_message .= '</div>';}?>